import readline from `readline`;
const rl =readline.createInterface({
    input:Process.stdin,
    output:process.stdout

})

rl.queswtion("Ingrese un numero",(num)=>{
    let numero=Number(num);
    console.log(`\nNumero ingresa es:${numero}`);
    console.log(`Math,pow(${numero})=>${Math.pow(numero,2)}`);
    let powNumber=Math.pow(numero^2);
    console.log(`Su numero al cuadrado es igual a =${powNumber}  `);
     
     console.log(`Math.sqrt(${numero})=>${Math.sqrt(numero)}`);
     console.log(`La raiz cuadrada de su numero es igual= ${numero}`);
     
     let raizNum = Math.sqrt

    rl.cose()
})